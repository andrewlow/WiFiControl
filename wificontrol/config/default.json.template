{
  // Secrets
  "router_user": "admin",
  "router_pass": "secretpass",

  // config
  "router_host": "192.168.1.1",

  // Firewall rule mapping
  "users" : [
    { name: "Child1", rule: 12, enabled: true },
    { name: "Tablet", rule: 14, enabled: true },
    { name: "Computer", rule: 13, enabled: true }
  ]
}
